{%- assign settings_name = name | upper_camel_case | append: "Settings" -%}
{%- assign screen_name = name | upper_camel_case | append: "Screen" -%}
use crate::prelude::*;

mod state;
mod screen;
pub mod prelude {
    pub use super::screen::{ {{settings_name}}, {{screen_name}} };
}

pub fn plugin(app: &mut App) {
    app.add_plugins((state::plugin, screen::plugin));
}
