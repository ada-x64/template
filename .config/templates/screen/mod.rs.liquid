{%- assign schedule_name = name | upper_camel_case | append: "Schedule" -%}
{%- assign screen_name = name | upper_camel_case | append: "Screen" -%}
use crate::prelude::*;

mod state;
mod systems;
pub mod prelude {
    pub use super::systems::{ {{schedule_name}}, {{screen_name}} };
}

pub fn plugin(app: &mut App) {
    app.add_plugins((state::plugin, systems::plugin));
}
