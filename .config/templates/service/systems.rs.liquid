use crate::prelude::*;

pub fn example() {
    // replace me!
}

/// Add this function to any screens which need to use this service.
pub fn systems() -> ServiceSystems {
    ServiceSystems::new((example))
}
